apiVersion: v1
kind: Secret

metadata:
  name: taskflow-secret
  namespace: taskflow

type: Opaque

stringData:
  DB_USER: "taskflow"
  DB_PASSWORD: "karuraforest"
  #JWT_SECRET: "your-super-secret-jwt-key"
  #REDIS_PASSWORD: "your-redis-password"